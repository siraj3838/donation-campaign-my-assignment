import { useLoaderData } from "react-router-dom";

const Home = () => {
    const donationData = useLoaderData();
    return (
        <div>
            {

            }
        </div>
    );
};

export default Home;